'''
要求：判断今年是否是闰年
问题分析：
1. 如何获取当年年份?
    用户手动输入，或者自动导入计算机当前年份。
2. 判断闰年的条件？
    年份能被400整除，或者年份能被4整除但不能被4整除。
'''

year = input("请输入年份：")
if eval(year) % 400 == 0 or eval(year) % 4 == 0 and eval(year) % 100 != 0:
    print("闰年！")
else:
    print("平年!")

